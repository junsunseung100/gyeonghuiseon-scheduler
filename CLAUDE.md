# CLAUDE.md — 경희선한의원 문진일정 달력

이 저장소에서 작업할 때 지킬 규칙. (전역 CLAUDE.md와 별개로 이 프로젝트에만 적용)

## 프로젝트

- 한약 처방 회차별로 확인전화·문진·마무리문자·재연락 일정을 자동으로 잡는 웹앱. 데스크 간호사와 원장이 함께 씀.
- 스택: Vite + TypeScript + Vitest + Supabase. 배포: Vercel(main에 push하면 자동 배포).
- 명령: 개발 `npm run dev` · 빌드 `npm run build` · 테스트 `npm test`. **커밋 전 test와 build 둘 다 통과 확인.**

## 도메인 규칙 (제일 중요 — 로직 바꾸기 전 반드시 읽기)

- 처방 번호 `X-Y`: X = 결제 개월수 × 2, Y = 블록 내 순번. 통산 회차는 별도. 결제=블록, 완주=블록 채움.
- 처방일 입력 시 자동 생성: **처방·문자(합쳐서 한 항목 `처방문자`)**, `확인전화`(+1, 같은 번호), `문진예정`(+10~12 중 휴진 아닌 가장 늦은 날, **다음** 번호).
- 확인전화가 휴진이면 다음 진료일로(수요일 처방→금요일). 문진예정 구간이 연휴로 다 막히면 앞으로 당김.
- 목요일(휴진일) 처방의 문자는 전날 예약.
- 마지막 회차(Y=X)는 문진예정 대신 마무리문자 1차(마지막 처방+12), 2차(1차+10). 내원 시 2차 삭제.
- 재연락은 **문진예정에만**: 3일 뒤 → 2회째부터 "원장에게 얘기하기" + 10일 뒤. 라벨 "N회차".
- 휴진 = 일·목·법정공휴일. 원외탕전 처방 가능일 = 일·공휴일 제외(목·토 가능), 토요일 지방/해외는 경고.
- **토요일 문진예정 최대 3개**(넘으면 금요일로 당기거나 월요일로). **일요일·공휴일에는 자동 일정이 안 잡히고 회색+공휴일 이름 표시, 메모만.**

## 화면·상태 규칙

- **완료된 일정은 달력에서 지우지 말고 회색으로 남긴다**(기록 유지). 오늘 할 일 목록에서는 뺀다.
- 오늘 할 일은 지난 일·오늘·다가올 일을 **가로 3열**로 보여준다.
- 달력은 일요일 칸을 좁게, 월~토 칸을 넓게.
- 로그인: Supabase 세션 유지 + **4자리 PIN** 게이트(빠른 재진입).
- 문자 보내기: 환자 전화번호로 **문자 앱을 미리 채워 여는 반자동** 방식(자동 발송 아님).

## 데이터·보안

- Supabase 표: patients, blocks, prescriptions, tasks, settings.
- RLS 켜짐(로그인 사용자만), 공개 가입 차단. 키는 `.env.local`(VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY) — **절대 커밋 금지.** Vercel에는 대시보드 환경변수.

## 코딩 규칙

- 날짜는 로컬 `YYYY-MM-DD` 문자열. `addDays`는 로컬 날짜 부품으로 포맷(toISOString 쓰면 한국시간에서 하루 밀림 — 실제 겪은 버그).
- 순수 로직(holidays/dates/schedule/recontact)은 반드시 테스트와 함께. UI는 main.ts.
- 삭제는 되돌리기(Ctrl+Z) 가능하게 스냅샷 저장.

## 하지 말 것

- 실제 문자·카카오톡 **자동 발송**(서비스·발신번호·심사 필요) — 지금 범위 밖. 반자동(문자 앱 미리채움)까지만.
- 증상(호전/유지/악화) 기록 추가(임상 정보라 이 일정 도구 범위 밖).
- `.env.local`·키 커밋.

## 수정 이력

- 2026-09-23: V2 계획 반영(완료 회색 유지, 처방·문자 합침, 일괄 완료, 공휴일 미리·일요일/공휴일 차단, 토요일 문진 3개, 오늘 3열, 달력 칸, PIN 로그인, 반자동 문자, 대시보드). 상세: `docs/V2_계획.md`.
